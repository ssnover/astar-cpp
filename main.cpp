#include "a-star.h"
#include <iostream>

int main()
{
    int width = 8;
    int height = 8;
    // make a list of nodes
    std::vector<std::pair<NODE, std::tuple<int>>> node_space;

    // then add all of the edges inside

    return 0;
}